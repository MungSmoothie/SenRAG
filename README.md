# SenRAG - 个人知识库 RAG 系统

🧠 基于 RAG (Retrieval-Augmented Generation) 的个人知识库系统，支持文档上传和智能问答。

## 功能特性

- 📤 **文档上传** - 支持 txt, md, pdf, json, yaml, html 等格式
- 💬 **智能问答** - 基于上传文档进行问答
- 🔧 **可配置** - 支持配置 LLM 和向量数据库
- 🐳 **Docker 部署** - 一键部署
- 🎨 **简洁界面** - Vue 3 + Vite

## 快速开始

### 1. 准备工作

确保已安装：
- Docker & Docker Compose
- Git

### 2. 克隆项目

```bash
git clone <your-repo-url>
cd SenRAG
```

### 3. 配置环境变量

```bash
cp .env.example .env
# 编辑 .env，填入你的 API Key
```

`.env` 文件内容：
```env
# OpenAI API (支持兼容 OpenAI 接口的其他服务)
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=sk-your-api-key
LLM_MODEL=gpt-3.5-turbo

# Qdrant (可选认证)
QDRANT_API_KEY=
```

### 4. 启动服务

```bash
docker-compose up -d
```

### 5. 访问系统

- 前端: http://localhost:3000
- API: http://localhost:8080
- Qdrant Dashboard: http://localhost:6333 (仅开发模式)

## 使用说明

### 上传文档

1. 打开前端页面
2. 拖拽或点击选择文件
3. 等待上传和处理完成

### 开始问答

1. 在输入框输入问题
2. 按 Enter 或点击发送
3. 查看回答结果

### 配置调整

访问 http://localhost:3000/config 可调整：
- LLM Base URL
- API Key
- 模型选择
- Qdrant 连接配置

## 技术架构

```
┌─────────────────────────────────────────────────────┐
│                   Frontend (Vue 3)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │  文档上传    │  │  智能问答    │  │   系统配置   │ │
│  └─────────────┘  └─────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────┐
│                   Backend (Go)                       │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │  REST API   │  │   RAG 服务   │  │  配置管理    │ │
│  └─────────────┘  └─────────────┘  └─────────────┘ │
└─────────────────────────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
    ┌─────────┐    ┌───────────┐    ┌─────────┐
    │  LLM    │    │ Embedder  │    │Qdrant  │
    │ (GPT)   │    │ (OpenAI)  │    │(向量库) │
    └─────────┘    └───────────┘    └─────────┘
```

## 本地开发

### 后端

```bash
cd backend
go mod download
go run main.go
```

### 前端

```bash
cd frontend
npm install
npm run dev
```

## API 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/health | 健康检查 |
| POST | /api/upload | 上传文档 |
| POST | /api/query | 问答请求 |
| POST | /api/query/stream | 流式问答 |
| GET | /api/config | 获取配置 |
| PUT | /api/config | 更新配置 |

## 配置说明

### config.yaml

```yaml
server:
  host: "0.0.0.0"
  port: 8080

llm:
  provider: "openai"
  base_url: "https://api.openai.com/v1"
  api_key: ""
  model: "gpt-3.5-turbo"
  max_tokens: 2000
  temperature: 0.7

embedder:
  model: "text-embedding-3-small"
  batch_size: 32

qdrant:
  host: "localhost"
  port: 6333
  api_key: ""
  collection_name: "senrag_collection"

upload:
  max_file_size: 52428800
  allowed_extensions:
    - ".txt"
    - ".md"
    - ".pdf"
  save_path: "./uploads"
```

## 目录结构

```
SenRAG/
├── backend/
│   ├── main.go
│   ├── config.yaml
│   ├── Dockerfile
│   ├── config/
│   ├── handlers/
│   ├── services/
│   └── rag/
├── frontend/
│   ├── src/
│   ├── Dockerfile
│   └── nginx.conf
├── docker-compose.yml
├── .env.example
└── README.md
```

## License

MIT
